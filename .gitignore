/target
/honr.json
/honr.json.tmp
.honr/
node_modules
web/dist
.DS_Store
web/tsconfig.tsbuildinfo

# UI screenshots, regenerated on demand
web/shots
.honr

# Local Dolt / sqlite artifacts (not board state under .honr/)
.dolt/
*.db

# Local sandbox vendor blobs (install via Containerfile / CLI)
sandbox/agy.tar.gz
sandbox/antigravity

# Local board backups
honr.json.pre-flat-backup
