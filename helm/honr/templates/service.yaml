apiVersion: v1
kind: Service
metadata:
  name: {{ include "honr.fullname" . }}
  labels:
    {{- include "honr.labels" . | nindent 4 }}
    {{- include "honr.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "honr.selectorLabels" . | nindent 4 }}
